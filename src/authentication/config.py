# Recursers can create their personal access token at https://www.recurse.com/settings/apps
# To use the tool, create your personal access token and replace the fake access token below.
PERSONAL_ACCESS_TOKEN = "123456"
AUTH_HEADER = {"Authorization": "Bearer {}".format(PERSONAL_ACCESS_TOKEN)}
